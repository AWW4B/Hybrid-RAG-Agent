// =============================================================================
// src/components/ChatHeader.jsx
// Kept as minimal export for backwards compatibility — not used in new layout.
// The header is now inline in App.jsx.
// =============================================================================
export default function ChatHeader() {
  return null
}
